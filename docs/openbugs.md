Double check CLI commands
Line number in logs changes depending on wrap


Log view; The breadcrumb shouldn't move when toggling on/off errors (⚠ 7  ✕ 6 appears and bumps breadcrumb left) 


Trigger pipeline opens last pipeline instead of the pending view

vim describe view whitespace inconsistent

`// vim: set filetype=groovy:` suffix at the end of vim files

Duration progress timer off by one? Seems to always go to +1

When following a build in the stages view, the artifacts in the header isn't automatically added when they're published. Not during, and not when finished.

Deleted jobs still show up

New jobs / folders don't show up automatically

Vim newline alignment puts tabs instead of spaces

